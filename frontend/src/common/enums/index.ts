export enum WalletEnum {
  Message = 'Welcome! By signing this message, you are verifying your identity. Nonce:',
}

export enum ContestTimeStatus {
  UPCOMING = 'upcoming',
  ACTIVE = 'active',
  ENDED = 'ended',
}

export enum ContestStatus {
  UPCOMING = 1,
  ONGOING = 2,
  ENDED = 3,
}
